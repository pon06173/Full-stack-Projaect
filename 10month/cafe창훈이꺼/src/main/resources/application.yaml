spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/cafe?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: gch918918!

  jpa:
      show-sql: true
      open-in-view: false
      generate-ddl: true
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          format_sql: true